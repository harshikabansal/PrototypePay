
// This file is being replaced by src/app/(app)/scan-qr/page.tsx
// It can be deleted.
// Keeping it temporarily to avoid breaking changes if not all references are updated immediately.
import { redirect } from 'next/navigation';

export default function OldClaimPage() {
  redirect('/scan-qr');
  return null;
}
