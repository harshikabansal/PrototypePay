import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFFD700); // Gold
  static const Color secondary = Color(0xFFFFA500); // Orange
  static const Color background = Colors.white;
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.grey;

  // Dark theme colors
  static const Color primaryDark = Color(0xFFFFD700); // Gold
  static const Color secondaryDark = Color(0xFFFFA500); // Orange
  static const Color backgroundDark = Colors.black;
  static const Color textPrimaryDark = Colors.white;
  static const Color textSecondaryDark = Colors.grey;
}
